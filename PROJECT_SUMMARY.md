# NAITE 채널 운영 대시보드 — 프로젝트 정리

## 1. 목적
NAITE 브랜드 9개 채널의 운영 현황(발행 루틴, 애드센스 승인, 조회수/수익, 예약 소진)을
한눈에 보는 정적 대시보드. Google 스프레드시트에 숫자만 입력하면 대시보드가 자동으로 반영.

## 2. 채널 구성

| ID | 채널 | 그룹 | 역할/메모 | 상태 |
|---|---|---|---|---|
| 1 | 네이버① | 강사·전문 | 강사 브랜딩 | 운영중 |
| 2 | 네이버② | 도서리뷰 | 도서 수수료 | 운영중 |
| 3 | 티스토리③ | 재테크 | 애드센스 승인대기 (⭐단기목표) | 운영중 |
| 4 | 티스토리④ | 강사·전문 | 애드센스 승인완료·핵심수익 | 운영중 |
| 5 | 스레드⑤ | 재테크 | 매일 자동운영 | 운영중 |
| 6 | 인스타⑥ (@naite.46) | 브랜딩 | 미시작·릴스 업로드 예정 (목표 주1~2개) | 미시작 |
| 7 | 카카오⑦ | 강사·전문 | 소식지 | 운영중 |
| 8 | 크몽⑧ | 강사·전문 | 전자책+첨삭 | 운영중 |
| 9 | 리틀리⑨ | 강사·전문 | 전자책 | 운영중 |

## 3. Google 스프레드시트

- **Sheet ID**: `1OT_DbYspkfbtNzYS-673GPZBy2-8VUgXGXyej9rPbSA`
- **공유 설정**: 일반 액세스 = "링크가 있는 모든 사용자" (뷰어) — 반드시 켜져 있어야 대시보드가 읽어옴
- **탭 3개**:

| 탭 이름 | gid | 컬럼 |
|---|---|---|
| `일별데이터` | 0 | 날짜, 채널, 게시글수, 조회수, 애드센스수익, 애드센스승인상태, 비고 |
| `주간루틴` | 429150204 | 주차시작일, 월(④), 화(③), 수(②), 목(④), 금(①), 비고 |
| `채널마스터` | 15164149 | 채널ID, 채널명, 그룹, 역할설명, 상태, 예약발행마감일 |

**입력 규칙**
- `일별데이터`: 한 행 = 하루 한 채널. `애드센스수익`은 채널④만, `애드센스승인상태`(승인대기/심사중/반려/승인완료)는 채널③만 입력
- `채널마스터.예약발행마감일`: 예약 포스팅을 큐에 올릴 때마다 "소진되는 날짜"로 갱신 → 대시보드가 D-day 자동 계산

## 4. 대시보드 기능

1. **🚨 지금 준비해야 할 채널** (최상단) — 운영중 채널을 예약 소진 D-day 오름차순 정렬. 3일 이하/미설정/경과 = 빨간 배지, 4~7일 = 노랑, 8일+ = 초록
2. **주간 발행 루틴 체크리스트** — 월=④ 화=③ 수=② 목=④ 금=①, 이번 주 완료 여부
3. **애드센스 승인 현황** — 채널③ 승인 상태를 배지로 강조 (단기 목표)
4. **그룹별 요약 카드** — 강사·전문/재테크/도서리뷰 최근 7일 게시글수·조회수·수익 + 브랜딩(채널⑥) 준비중 카드
5. **채널별 추이 그래프** — 그룹 필터 + 주간/월간 토글, Chart.js 라인 차트
6. **원본 데이터 표** — 접기/펼치기, 최근 200행
7. 모바일 반응형(360px~), 라이트/다크 테마 토글(우측 상단 🌓, localStorage 저장)

## 5. 기술 아키텍처 & 해결한 이슈

- **스택**: 순수 HTML/CSS/JS (빌드 없음), Chart.js는 CDN
- **데이터 로딩 방식**: 처음엔 Google Sheets "웹에 게시(CSV)" + Papa Parse로 시도했으나, 브라우저에서
  `Access to XMLHttpRequest ... blocked by CORS policy: No Access-Control-Allow-Origin header` 에러 발생
  (curl/서버사이드 fetch는 되지만 실제 브라우저 fetch/XHR는 막힘).
  → **Google Visualization API(`/gviz/tq`)를 `<script>` 태그 JSONP 방식**으로 호출하도록 전환해서 해결.
  이 방식은 "웹에 게시" 없이 "링크가 있는 사용자 - 뷰어" 공유만으로 동작함.
- gviz가 날짜 셀을 `Date(2026,7,4)` 형태(월 0-indexed)로 반환하므로 `gvizCellToDateStr()`에서 ISO 문자열로 변환
- CSV_URLS 방식은 폐기, 현재는 `SHEET_ID` + `SHEET_GIDS`만 설정하면 됨

## 6. 배포 정보

- **GitHub 저장소**: https://github.com/NAITE2288/channel (Public)
- **GitHub Pages URL**: https://naite2288.github.io/channel/
- **브랜치**: `main`, 루트(`/`)에서 배포

## 7. 파일 구조

```
naite-dashboard/
├── index.html              # 페이지 골격
├── assets/
│   ├── config.js            # SHEET_ID, SHEET_GIDS, 채널/그룹/루틴/상태 설정값
│   ├── data.js               # gviz JSONP 로드 + 데이터 정규화/집계 함수
│   ├── dashboard.js          # 렌더링 로직 (섹션별)
│   ├── sample-data.js        # SHEET_ID 미설정 시 미리보기용 샘플 데이터
│   └── style.css              # 라이트/다크 테마, 반응형 CSS
├── templates/                # 시트 3탭용 CSV 템플릿 (최초 세팅용)
├── preview-server.ps1        # 로컬 미리보기용 PowerShell 정적 서버 (node/python 없는 환경 대응)
└── README.md                  # 셋업 가이드
```

## 8. 향후 계획 (요청은 받았으나 아직 미착수)

운영 안정화 후 아래 항목 자동화 검토:
- **애드센스 수익(채널③④)**: Google AdSense Management API + OAuth 필요
- **스레드⑤ 게시글수/조회수**: Meta Threads/Graph API + 앱 등록·심사 필요
- 둘 다 정적 사이트만으로는 불가 — 토큰을 다루는 서버리스 함수 등 별도 백엔드 필요. 착수 시 구조를 다시 설계해야 함

## 9. 알려진 주의사항

- 스프레드시트가 "링크가 있는 사용자"에게 공개되어 있어 **링크를 아는 사람은 수익 데이터를 볼 수 있음** (검색 노출은 안 됨)
- 채널⑥(인스타)은 "운영 예정" 상태라 그룹별 요약 3종 카드에는 포함 안 되고 별도 "준비중" 카드로만 표시됨
- 데이터(조회수 등)는 전부 수동 입력 — 플랫폼별 API 부재로 완전 자동화는 현재 불가능
